<div class="container">
  <div class="well">
  <h1 class="text-center categories-title">All Categories</h1>
  <ul class="categories-list">
    <% @categories.each do |category| %>
      <h4><%=link_to "#{category.title}", category_path(category.id) %></h4>
      <!-- <h4><%#= "#{category.title}" %></h4> -->
    <% end %>
  </ul>
  </div>
</div>
