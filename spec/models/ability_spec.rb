require "cancan/matchers"

RSpec.describe Ability, :type => :model do
  # let(:ability) { create(:user) }
  #
  # describe "User" do
  #   describe "abilities" do
  #     subject(:ability){ Ability.new(user) }
  #     let(:user){ nil }
  #
  #     context "when user is a store admin" do
  #       let(:user){ Factory(:store_admin) }
  #
  #       it{ should be_able_to(:manage, Vendor.new) }
  #     end
  #   end
  # end
end
